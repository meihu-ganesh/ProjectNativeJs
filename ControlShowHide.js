import React from "react";
import { Text, View } from "react-native";

const ControlShowHide = () => {

    return (
        <View>
            <Text style={{fontSize: 30, color: "#ad09bc"}}>User Component</Text>
        </View>
    );
};

export default ControlShowHide;
